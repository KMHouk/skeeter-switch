{
  "name": "skeeter-switch-functions",
  "version": "1.0.0",
  "description": "Azure Functions for skeeter-switch mosquito control system",
  "main": "dist/functions/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "npm run build && func start",
    "prestart": "npm run build"
  },
  "dependencies": {
    "@azure/data-tables": "^13.2.2",
    "@azure/identity": "^4.0.0",
    "@azure/keyvault-secrets": "^4.7.0",
    "@azure/monitor-opentelemetry": "^1.3.0",
    "@azure/functions": "^4.5.0",
    "date-fns": "^3.6.0",
    "date-fns-tz": "^3.1.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "engines": { "node": ">=18.0.0" }
}
